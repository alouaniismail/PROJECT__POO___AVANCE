Dans les tests de simple, pas de import tec.*, pour faire des greffons comme le lien est UN on doit inclure une fonction statique dans le FabriqueTec.java et importer collecte.* et dans le lien a-UN faire la même chose car cela consiste en faire une initialisation de GreffonAUn. Partons depuis le début, il nous faut des passagers, pas de problemes. Le vehicule renseigne la collection, il faut agir ici, le constructeur doit avoir un vehicule v2 et un collecteur c2, dans fabriqueTEC dans le package tec, on doit avoir une fonction donc de nature statique qui prend en profondeur un nbPlace Asiise et un nbrPlaceDebout et une collection col et qui fera la même chose que (v2,c2) soit return: new GreffonAUn( [selon le fichier GreffonAUn.tec] [....]) avec comme ARGS:
un vehicule qui sera new Autobus(nbPlaceAssise, nbPlaceDebout) et le deuxième argument la col dans FabriqueTec dans le package tec qui importe le package collecte(importe collecte.*;). ATTENTION A CELA. 
PROJET FINALISE. 
